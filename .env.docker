# Server
NODE_ENV=production
PORT=8080
HOST=0.0.0.0

# Sessions / Redis
SESSION_SECRET=change-me-strong-random
REDIS_HOST=redis
REDIS_PORT=6379
# (If your code expects REDIS_URL instead of host/port, use:)
# REDIS_URL=redis://redis:6379

# Cookies (HTTPS in prod)
COOKIE_SAMESITE=lax
COOKIE_SECURE=true

# MySQL (use the Service DNS name)
MYSQL_HOST=mysql
MYSQL_PORT=3306
MYSQL_USER=app_user
MYSQL_PASSWORD=app_password
MYSQL_DATABASE=app_db


